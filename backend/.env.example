ASSEMBLYAI_API_KEY=
ASSEMBLYAI_VOICE_AGENT_URL=wss://agents.assemblyai.com/v1/ws

# Comma-separated list of origins allowed to call this backend's REST API
# (CORS). Set to your deployed frontend's exact origin(s) in production,
# e.g. https://your-app.vercel.app — "*" (the default) is fine for testing.
ALLOWED_ORIGINS=*